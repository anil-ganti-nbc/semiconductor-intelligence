# support_status: BROKEN — products.json returns Unavailable Shop
# Audited 2026-08-02. Do not enable without re-probe.
# Platform unverified (probe returned nothing from sandbox).
# Run `oem-radar probe https://www.trigkey.com` on your machine, then set enabled: true.

manufacturer:
  name: Trigkey
  country: CN

sources:
  - id: trigkey-shopify
    engine: shopify
    base_url: https://www.trigkey.com
    min_interval: 12h
    discovery: [products_json, sitemap]
    currency_default: USD
    enabled: false
