# support_status: BROKEN — geekompc.com returns HTML 404; geekom.com lander redirect — not Shopify
# Audited 2026-08-02. Do not enable without re-probe.
# GEEKOM — UNVERIFIED from sandbox (limited egress). On your machine run:
#   oem-radar probe https://www.geekompc.com
# If it reports 'shopify', set enabled: true below. If 'woocommerce' or
# 'unknown', see docs/BIG_BRANDS.md (may need a different engine).
manufacturer:
  name: GEEKOM
  country: TW
sources:
  - id: geekom-shopify
    engine: shopify
    base_url: https://www.geekompc.com
    min_interval: 12h
    discovery: [products_json, sitemap]
    currency_default: USD
    enabled: false
